# Rust build artifacts
# (unanchored so it also catches nested crates, e.g. ad-network-server/target/ —
# a nested target/ dir was previously committed by accident; see git history)
target/
**/*.rs.bk
*.pdb
Cargo.lock

# IDE files
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Environment files
.env
.env.local
.env.*.local

# Logs and runtime files
*.log
.log/
.trust-sidecar.log
.ad-network.log
.test-server.log

# PID files
.pid
*.pid
.trust-sidecar.pid
.ad-network.pid
.test-server.pid

# Documentation build
/doc

# Test artifacts
*.profdata
*.tmp
*.bak

# OS files
Thumbs.db

# Internal-only working notes (not for public repo)
/review.md
